	public void readPlayers(){
		FileReader fr = null;
		
		BufferedReader br=null;
		
		File score = new File("src/score.csv");
		if(score.exists()){
			//System.out.println("FIle exists");
		}
		else{
			try {
				score.createNewFile();
				//System.out.println("File created");
			} catch (IOException e) {
				e.printStackTrace();
			}
		}
		
		try{
			fr=new FileReader(score);
			br=new BufferedReader(fr);
			 
			String readLine;
			String []data;
			int numOfPlayers=0;
			
			while( (readLine=br.readLine())!=null ){
				
				if(firstLine==false){
					data=readLine.split(",");
					numOfPlayers=Integer.parseInt(data[2]);
					firstLine=true;
				}
				else{		 
					data=readLine.split(",");
				 
					
				    Player  player= new Player(data[0],Integer.parseInt(data[1]));
				    ListOfPlayers.players.add(player);
			 
				 
				}
			}
		}catch(Exception e){
			System.out.println("Exception caught 1");
			e.printStackTrace();
		}finally{
			if(br!=null)
				try {
					br.close();
				} catch (IOException e) {
					System.out.println("Exception caught 2");
					e.printStackTrace();
				}
		}
		
	}